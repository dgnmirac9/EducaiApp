components.jsx ve styles.css dosyalarında şu değişiklikleri yap:

1. Yıldızları kaldır:
ProfileCard'daki .xp-medal içindeki .medal-stars div'ini tamamen kaldır.
styles.css'te .medal-stars ile ilgili tüm kuralları sil.

2. Sidebar butonlarını Türkçeleştir ve fonksiyon ata:
Sidebar component'indeki nav item'larını bul ve şu şekilde güncelle:
- Home → "Ana Sayfa" — onClick: navigate('/') veya setActive('home')
- Subjects → "Dersler" — onClick: setActive('subjects') + scroll to subjects section
- Challenges → "Görevler" — onClick: setActive('challenges')
- Quests → "Düello" — onClick: navigate('/duel')
- Profile → "Profil" — onClick: navigate('/profile')
- Notifications → "Bildirimler" — onClick: alert('Yakında!')
- Settings → "Ayarlar" — onClick: alert('Yakında!')

3. ChallengesCard'daki "Katıl" butonu:
ChallengesCard component'ini bul.
Her challenge kartındaki "Katıl" butonuna:
- Challenge adı "Test Düellosu" veya herhangi bir düello ise:
  onClick: navigate('/duel')
- Genel challenge için:
  onClick: navigate('/duel') 

4. useNavigate hook'unu Sidebar ve ChallengesCard'a ekle:
import { useNavigate } from 'react-router-dom'
const navigate = useNavigate()

Kurallar:
- Mevcut tasarıma dokunma
- Sadece onClick'leri ve Türkçe isimleri güncelle
